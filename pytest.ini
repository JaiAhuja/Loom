[pytest]
testpaths = tests
markers =
    unit: fast tests for isolated functions and classes
    integration: tests that combine service modules with faked external adapters
    e2e: end-to-end tests covering a complete user-facing workflow
